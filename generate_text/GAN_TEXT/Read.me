This Model structure according to the paper
<Adversarially Learned One-Class Classifier for Novelty Detection>

R(reconstructor):
    decoder-encoder, use CNN

D(Detector):
    same as R's encoder, use CNN
